spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: false

server:
  port: ${PORT:8088}

ms-pedidos:
  url: ${MS_PEDIDOS_URL}
ms-clientes:
  url: ${MS_CLIENTES_URL}
